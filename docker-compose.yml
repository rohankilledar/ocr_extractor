version: '3.8'

services:
  fastapi-ocr-app:
    image: fastapi-ocr-app
    ports:
      - "8000:8000"
    environment:
      - OPENAI_API_KEY= YOUR_API_KEY
#     secrets:
#       - openai_api_key

# secrets:
#   openai_api_key:
#     external: true
